const columnNameInputField = (title) => {
  return `<input type="text" class="colname-fix" required class="card-tittle-input" placeholder="이름을 입력하세요" value="${title}" / >`;
};

export { columnNameInputField };
